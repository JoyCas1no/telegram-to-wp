- name: Run VK to WP script
  run: python scripts/import_vk_to_wp.py
  env:
    VK_TOKEN: ${{ secrets.VK_TOKEN }}
    WP_USERNAME: ${{ secrets.WP_USERNAME }}
    WP_PASSWORD: ${{ secrets.WP_PASSWORD }}
